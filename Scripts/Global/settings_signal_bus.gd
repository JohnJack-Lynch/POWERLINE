extends Node

signal _window_mode_selected(index : int)
signal _resolution_selected(index : int)

signal _reticle_enabled(value : bool)
signal _arrow_enabled(value : bool)

signal _set_settings_dict(settings_dict : Dictionary)

signal _load_settings(settings_dict : Dictionary)
